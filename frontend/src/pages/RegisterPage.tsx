// Both auth routes render the same AuthPage component so the sliding
// panels stay mounted (and animate) when switching between login/register.
export { AuthPage as RegisterPage } from './AuthPage'
